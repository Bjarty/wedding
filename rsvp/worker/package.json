{
  "name": "@lisette-bjarty/rsvp-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test test/production-config.test.mjs test/provision-household-code.test.mjs test/worker.test.mjs",
    "provision:household-code": "node scripts/provision-household-code.mjs",
    "provision:invitation": "node scripts/provision-invitation.mjs"
  },
  "devDependencies": {
    "typescript": "~5.8.2",
    "wrangler": "4.126.0"
  }
}
